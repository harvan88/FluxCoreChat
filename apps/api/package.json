{
  "name": "@fluxcore/api",
  "version": "0.1.0",
  "description": "FluxCore API - Backend con Elysia",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "dev:simple": "bun run --watch src/index.ts",
    "build": "bun build src/server.ts --outdir dist --target bun",
    "start": "bun run dist/server.js",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@elysiajs/cors": "0.8.0",
    "@elysiajs/jwt": "0.8.0",
    "@elysiajs/swagger": "0.8.5",
    "@fluxcore/db": "workspace:*",
    "@fluxcore/types": "workspace:*",
    "bcrypt": "^6.0.0",
    "elysia": "0.8.17"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "bun-types": "^1.0.0",
    "typescript": "^5.3.3"
  }
}
